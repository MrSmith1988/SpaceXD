float* getItemPos(int itemID) {
  float itemPos[3]; 
  // Compute item position. Return pointer to the array. A pointer is the memory address of the float. 
  // The return value will be computed by taking the pointer and adding until we reach the last element of the array. 
  
  return itemPos;
}
